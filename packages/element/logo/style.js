import { css } from "standard/dom";

function style(self) {
  return css`
    img {
      aspect-ratio: 1 / 1;
      display: flex;
      height: 44px;
      width: 44px;
    }
  `;
}

export default style;
