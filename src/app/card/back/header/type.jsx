import global from '@standard/global'
import h from '@standard/h'
import text from '@elements/text'

function component () {
  return (
    <text.Span master xxxs>{global.type}</text.Span>
  )
}

export default component
