import h from '@standard/h'
import text from '@elements/text'

function component (_props, children) {
  return (
    <text.H2 master dark xxs center>{children}</text.H2>
  )
}

export default component
