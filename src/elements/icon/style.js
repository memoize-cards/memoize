import css from '@standard/css'

const style = css`
  .icon {
    color: var(--color-primary);
    font-size: 24px;
  }

  .icon[small] {
    font-size: 16px;
  }

  .icon[large] {
    font-size: 32px;
  }
`

export default style
