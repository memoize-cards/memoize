import css from '@standard/css'

const style = css`
  .setNewPassword__main {
    height: 100vh;
    margin: 0 auto;
    max-width: 408px;
    padding: var(--spacing_inset-xs);
  }

  .setNewPassword__resetPassword {
    width: 100%;
  }
`

export default style
