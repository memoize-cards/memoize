import css from '@standard/css'

const style = css`
  .signUp__main {
    height: 100vh;
    margin: 0 auto;
    max-width: 408px;
    padding: var(--spacing_inset-xs);
  }

  .signUp__getStarted {
    width: 100%;
  }
`

export default style
