import css from '@standard/css'

/**
 * CSS-in-JS tokens for font family values.
 */
const style = css`
  :host {
    --font-family-highlight: 'Inter', sans-serif;
    --font-family-base: 'Open Sans', sans-serif;
  }
`

export default style
