import { html } from "standard/dom";

function component() {
  return html`
    <section>
      <mc-text size="display-xl">Aprenda 5x mais rápido!</mc-text>
    </section>
  `;
}

export default component;
