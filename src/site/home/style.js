import css from '@standard/css'

const style = css`
  .home__main {
    height: 100vh;
    margin: 0 auto;
    max-width: 408px;
    padding: var(--spacing_inset-xs);
  }

  .home__getStarted {
    cursor: pointer;
    width: 100%
  }
`

export default style
