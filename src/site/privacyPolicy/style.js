import css from '@standard/css'

const style = css`
  .privacyPolicy__main {
    margin: 0 auto;
    max-width: 408px;
    padding: var(--spacing_inset-xs);
  }

  .privacyPolicy__footer {
    height: 42px;
  }
`

export default style
