import css from '@standard/css'

const style = css`
  .termsOfUse__main {
    margin: 0 auto;
    max-width: 408px;
    padding: var(--spacing_inset-xs);
  }

  .termsOfUse__footer {
    height: 42px;
  }
`

export default style
